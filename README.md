# 小红书图文素材助手

## 文件夹说明

- `extension/`：发布插件用。Chrome / Edge 加载扩展或打包上传时，只选择这个文件夹。
- `docs/`：项目说明、隐私说明、测试说明、发布检查报告。
- `test/`：核心逻辑自动化测试。
- `e2e/`：端到端测试。
- `release/`：打包后的压缩包。

## 发布

手工上传插件时，使用 `extension/` 文件夹。压缩包里的根目录必须直接包含 `manifest.json`。
